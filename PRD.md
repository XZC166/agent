# 产品需求文档 (PRD) - 仿真场景自动生成智能体系统

## 1. 项目背景
本项目旨在开发一个基于单智能体（Single Agent）和检索增强生成（RAG）技术的系统，用于自动生成仿真场景（特别是 OpenFOAM/BlastFoam 相关的仿真案例）。系统利用现有的 OpenFOAM 教程描述作为知识库，根据用户的自然语言需求，自动规划并生成相应的仿真配置文件和目录结构。最近系统升级方向为增强智能体的自反思能力与环境交互能力，实现端到端的自动化联调。

## 2. 项目目标
- **自动化生成**：通过 LLM 理解用户需求，自动生成复杂的仿真配置文件。
- **知识复用**：利用 RAG 技术检索相似的历史案例（Tutorials），提高生成的准确性和规范性。
- **端到端执行**：智能体不仅生成代码/文本，还能直接在文件系统中创建文件和文件夹，完成场景搭建。
- **自主验证与闭环纠错 (新增)**：基于用户的验证指令，系统能在后台自动调度 BlastFoam 求解器进行案例试运行。通过采集并解析终端错误日志，智能体能够自主定位原因、修改文件并反复验证，直到案例成功运行。

## 3. 用户流程
1. **输入**：用户提供自然语言描述的需求（例如：“建立一个3D建筑物爆炸模拟...”）以及输出的目标路径。
2. **检索**：系统通过 RAG 模块在 `rag_database/openfoam_tutorials` 中检索最相关的案例描述。
3. **规划与生成**：智能体结合用户需求和检索到的参考案例，生成执行计划，并调用文件工具创设场景。
4. **触发验证 (新增)**：生成结束后，用户在多轮交互终端下达指令（例如：“运行当前案例，如果有报错请直接分析并修改”）。
5. **循环纠错与执行 (新增)**：系统在后台启动子进程运行对应的仿真命令，并进行监控。
   - 若运行成功，向用户汇报仿真成功。
   - 若出现报错（如字典文件语法错、网格错误或 Floating point exception），系统自动抓取部分核心错误日志并抛给智能体。
   - 智能体进行自反思，重新修改引发错误的源文件后，自动再次拉起求解器验证，直至成功或达到最大重试次数（目前设为 3 次）。

## 4. 功能需求

### 4.1 RAG 模块
- **知识库构建**：能够读取 `rag_database/openfoam_tutorials` 下的 Markdown 文件，并进行向量化存储。
- **检索功能**：根据用户输入的 Query，计算相似度，返回最相关的案例内容。
- **实现细节**：目前基于 `rag_database/embedder.py`，使用 OpenAI 接口进行 Embedding。

### 4.2 智能体 (Agent) 模块
- **核心逻辑**：接收“用户需求” / “案例生成要求” / “报错反馈信息” 作为上下文 (Context)。
- **能力**：
    - **思考 (Reasoning)**：分析生成需求，或者针对报错日志进行溯源归因与解决策略规划。
    - **文件工具 (File Tools)**：创建目录、写入文件内容的现有工具。
    - **命令行工具 (Shell Tools)(待开发)**：允许大模型下发如 `./Allrun`, `blastFoam`, `checkMesh` 等命令，并获得返回值的工具。
    - **读文件/读日志工具 (Read Tools)(待开发)**：在日志巨大时能灵活读取或过滤异常堆栈信息的工具。
- **框架**：使用 **LangChain** 的 AgentExecutor 框架开发多轮问答。

### 4.3 自动化验证与监控模块 (新增模块)
- **求解器调度**：通过 Python 的 subprocess 或 pexpect 等技术挂载系统命令，在子进程中执行后台任务，实时采集 stdout 与 stderr 的流数据。
- **智能输出截断**：OpenFOAM 求解产生的日志通常长达数万行。监控模块需具备“日志智能截断”策略，当捕捉到核心报错如 `FOAM FATAL ERROR`、异常终止状态码或连续浮点异常时，选取前后关键行组装返回给模型，避免上下文 Token 超缩超限。
- **自动迭代循环**：把原本单纯的一次生成变成“行动 -> 评估 -> 修正反馈 -> 再次行动”的闭环机制（Self-Healing）。最大自主纠错次数设置为 **3 次**。

### 4.4 交互接口
- **输入方式**：纯命令行工具 (CLI)，支持连续的多轮对话。
- **输出反馈**：实时打印智能体的“思考”、“工具调用”和“尝试执行命令与报错评估”的状态，提供充足的透明度。
- **用户干预**：允许用户在漫长的排错过程中随时输入终止指令并人工介入。

## 5. 待确认问题 (Open Questions)
- [✅] **交互形式**：纯命令行工具 (CLI)。
- [✅] **智能体框架**：LangChain。
- [✅] **多轮对话**：支持。
- [✅] **容错重试上限 (新)**：设置固定的最大自主纠错次数为 **3 次**，防止无限制的死循环调用消耗 Token。
- [ ] **执行安全与资源防卡死 (新)**：由于 OpenFOAM 可能会因为参数错乱卡死内存，关于设置执行时的硬件超时(Timeout)限制及计算性能限制机制，在后续功能优化迭代中再行考虑。
