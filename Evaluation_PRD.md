# 智能仿真Agent系统多维评测 PRD 

## 1. 背景与评测目的
经过近期的一系列重构，系统已经集成了 **Hybrid RAG（稠密向量 + BM25 稀疏检索及 RRF 融合打分）** 以及 **包含自主截断、超时控制、3次容错机制的自动化验证与监控模块**。
本 PRD 旨在建立一套标准化、可量化的评测准则，从**检索底层、系统全链路、智能体行为**三个维度对该系统的健壮性、精准度和自治能力进行综合评估。

---

## 2. 评测维度一：检索器的评估 (Retriever Evaluation)
**目标**：评估混合检索（Hybrid Search）架构在面对复杂、特定领域（OpenFOAM/blastFoam）的工程自然语言查询时，找回对应经验案例（教程 Markdown/JSON）的能力。

### 2.1 评测指标 (Metrics)
*   **Recall@K (召回率 / 命中率)**：在 Top-K（如 K=1, 3, 5）的检索结果中，是否包含了正确的目标参考案例。
*   **NDCG@K (归一化折损累计增益)**：不仅考虑文档是否相关，还考虑相关的程度（采用多级别打分，如 0=无关，1=求解器匹配，2=物理/几何要求部分匹配，3=完全高度匹配）以及文档的排名顺序。这对于仿真场景极其重要，因为某些参考案例哪怕不是最优解，也能提供部分有价值的配置片段。
*   **MRR (Mean Reciprocal Rank / 平均倒数排名)**：正确参考案例在检索结果中出现的最高排名的倒数均值。用于衡量检索框将最相关结果排在首位的能力。
*   **RRF 融合有效性诊断 (A/B Test)**：对比 **纯 Dense 检索** 与 **BM25 + Dense RRF 检索** 的得分，验证当 Prompt 包含极具体的专有名词（如“building3D”, “Murnaghan”）时，混合检索是否显著提升了打分精度。

### 2.2 测试用例设计 (Test Dataset)
构造 10-20 条具有典型代表性的 Query，分为：
1.  **功能场景类**：“帮我建立一个水下爆炸仿真案例，参考 underwater_blast。”
2.  **物理参数类**：“建立一个带有 JWL 状态方程的 C4 炸药气爆模型。”
3.  **模糊描述类**：“我想做一个 L 型建筑被高压气体冲击的 3D 模拟，需要用到 snappyHexMesh。”（测试语义匹配能力）

---

## 3. 评测维度二：端到端评估 (End-to-End Evaluation)
**目标**：从用户输入提示词（Prompt）开始，到最终在目标机器上生成**结构完整、通过前处理校验、且求解器可稳定运行**的仿真案例的整体转化能力。

### 3.1 评测指标 (Metrics)
*   **生成合规率 (Format Compliance Rate)**：系统最终输出的文件和目录结构（包含 `0`, `constant`, `system` 文件夹以及 `Allrun`, `Allclean` 脚本）是否 100% 满足 OpenFOAM 基本要求，无遗漏字典文件。
*   **首次验证成功率 (First-Pass Run Rate)**：智能体第一次执行 `./Allrun` 时，是否能一次性毫无报错地通过所有前处理组件（`blockMesh`, `snappyHexMesh`, `setFields`）和生成有效网格。
*   **端到端结案成功率 (Final Runnable Rate)**：**最核心指标**。允许经过最大 3 次自修复后，求解器 (`blastFoam`) 是否能够正常拉起计算，且物理状态稳定（日志无 `FOAM FATAL ERROR`，允许 300s 触发超时视为物理计算中）。
*   **端到端时间耗用 (End-to-End Latency)**：从接收请求到反馈最终结果的总体耗时，评估商业化落地的用户等待体验。

### 3.2 评测边界条件
*   环境：需要严格在带有 OpenFOAM 9 / blastFoam 安装环境的主机中运行测试。
*   测试域：默认在拥有写入权限的路径（如 `/tmp`）下随机生成沙盒测试环境。

---

## 4. 评测维度三：智能体评估 (Agent Evaluation)
**目标**：剥离具体的生成结果，单独评估 Agent 底层的 ReAct 思维链逻辑、工具选取准确性、错误诊断的“心智模型”及自愈能力。

### 4.1 评测指标 (Metrics)
*   **工具调用准确率 (Tool Call Accuracy)**：
    *   在需要建文件夹时是否准确调用了 `create_directory`。
    *   在审查错误日志时是否使用了带截断保护的 `read_log_file`。
    *   是否发生过死循环调用同一工具的幻觉错误。
*   **错误自主诊断与修正成功率 (Self-Healing Success Rate)**：
    *   **触发条件**：人为注入错误（如修改 prompt 引导其生成错误的 `fvSchemes`），或监控生成自然发生的错误（如 `Permission denied`，拼写错误等）。
    *   **评估点**：Agent 是否正确识别了错误原因（例如：“发现 Allrun 缺乏执行权限，我将使用 chmod 修复”）。
    *   **修正率**：在容错重试上限（3次）内，成功将错误状态转化为 Runnable 状态的成功比率。
*   **上下文文脉保持能力 (Context Retention)**：在经过长达十几次的工具交替调用（写文件 -> 运行 -> 读错 -> 改文件）后，Agent 是否会遗忘用户的原始需求（如忘记网格尺寸需要设为 10x10，或是遗忘放置测点/探针的要求）。
*   **环境适应与规划合理性 (Planning & Exploration Logic)**：遇到 `workspace 权限拒绝` 时，是否能自主探测系统探寻 `/tmp` 目录等替代方案（这一项我们在昨天已经成功验证一次）。

---

## 5. 评测实施方案 (Execution Plan)

### 5.1 第一阶段：自动化测试工具开发 (Test Scripting)
建立一个自动化的评测 Python 脚本 `evaluate.py`，结构如下：
1.  **输入池**：读取预设的几十个 Prompt JSON 列表。
2.  **执行器**：分别触发不同的模块。调用 Embedder 检查 Top-1 成功率并记录日志；实例化 Agent 检查生成全流程。
3.  **评分探针**：在生成的案例中跑探测脚本检查是否存在必要的边界条件和 `log.blastFoam` 的运行结果。

### 5.2 第二阶段：人工辅助审阅 (Human Verification)
因为 OpenFOAM 的错误千奇百怪，系统跑完后生成的数据，需要具备一定流体力学背景的工程师进行复验：
*   **物理合理性审查**：虽然没有报错，但是炸药是否像要求一样放置在了建筑外部？(`setFieldsDict` 中的坐标复核)。
*   **边界条件冲突审查**：某些隐性错误求解器不报错，但结果是发散的（比如除以 0），这一项需要人工提取 Agent 判断的“成功案例”抽样复核。

### 5.3 报告输出格式预定
测试完成后输出评估矩阵图表：
*   ✅ **Retrieval Top-1 Acc**: xx%
*   ✅ **Agent Tool Call Acc**: xx%
*   ✅ **Self-Healing Resolution Rate**: xx% (以 Error 触发次数为分母)
*   ✅ **System End-to-End Success**: xx%
